import { SortAnswersByPipe } from './sortAnswersBy.pipe';

describe('SortAnswersByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAnswersByPipe();
    expect(pipe).toBeTruthy();
  });
});
